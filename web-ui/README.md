# web-ui

FileHub Web UI (Vue3 + Vite + Element Plus).

Dev:

```bash
npm ci
npm run dev
```

Build:

```bash
npm ci
npm run build
```

The production build is embedded into the Go backend via `web/dist` (see repository root `README.md`).
